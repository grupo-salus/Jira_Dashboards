// Arquivo vazio - será implementado futuramente

// Tipos para a feature Sprint
// Serão implementados conforme as rotas do backend forem criadas

export interface SprintApiError {
  erro: string;
}
